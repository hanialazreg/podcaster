import React from "react";

const Podcast = ({
  title,
  description,
  imgURL,
  podcastId,
}: {
  title: string;
  description: string;
  imgURL: string;
  podcastId: number;
}) => {
  return <div>{Podcast.name}</div>;
};

export default Podcast;
