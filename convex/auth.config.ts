// https://many-crayfish-42.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://many-crayfish-42.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
